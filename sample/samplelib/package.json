{
  "name": "samplelib",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "jest --watch",
    "lint": "tslint -p .",
    "build": "tsc-npm-bundler build",
    "testWithCoverage": "jest --coverage",
    "lintFix": "tslint -p . --fix",
    "prepublish": "npm run lint"
  },
  "author": "",
  "license": "ISC",
  "private": true,
  "devDependencies": {
    "@types/chai": "^4.1.7",
    "tslint": "^5.18.0",
    "tslint-config-standard": "^8.0.1",
    "typescript": "^3.5.3"
  },
  "main": "bin/bundle.js",
  "types": "bin/index.d.ts",
  "dependencies": {
    "tsconfig-paths-jest": "0.0.1",
    "tslib": "^1.10.0"
  }
}
